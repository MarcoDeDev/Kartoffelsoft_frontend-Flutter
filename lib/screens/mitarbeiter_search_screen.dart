// lib/screens/mitarbeiter_search_screen.dart

import 'package:flutter/material.dart';

class MitarbeiterSearchScreen extends StatelessWidget {
  const MitarbeiterSearchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Mitarbeitersuche wird hier implementiert.'),
    );
  }
}