import 'package:flutter/material.dart';

class KasseScreen extends StatelessWidget {
  const KasseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Kasse Bildschirm'),
      ),
    );
  }
}