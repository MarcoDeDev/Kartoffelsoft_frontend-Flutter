import 'package:flutter/material.dart';

class LagerScreen extends StatelessWidget {
  const LagerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Lager Bildschirm'),
      ),
    );
  }
}